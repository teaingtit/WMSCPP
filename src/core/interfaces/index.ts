/**
 * Core Interfaces - Barrel Export
 *
 * @description
 * Central export point for all repository interfaces (Ports).
 *
 * @architecture Clean Architecture - Core Layer (Ports)
 */

export type { IInventoryRepository } from './IInventoryRepository';
